/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{

	let nombre = prompt("ingrese un nombre");
	alert( "el nombre ingresado es: " + nombre);
}

// Prompt = funcion de jasvascrip que nos deja escribir un dato que al poner aceptar nos devuelve lo que el usuario coloco. 

//alert = recibe un dato y no devuelve nada